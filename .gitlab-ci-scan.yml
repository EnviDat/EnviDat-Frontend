
verify-node-audit:
  stage: test
  image: node:20.15.0-alpine3.20
  environment: $CI_COMMIT_REF_NAME
  extends: .normal-rules
  needs:
    - job: modify-env
      artifacts: false
  script:
    # keep this in separate job to clearer identify if there is a problem
    - npm audit --audit-level=high
  retry:
    max: 2


verify-node-owasp:
  stage: test
  image: owasp/dependency-check:latest
  environment: $CI_COMMIT_REF_NAME  
  rules:
    - when: manual
  needs:
    - job: modify-env
      artifacts: false
  script:
    # dependency check against the National Vulnerability Database
    # https://dependency-check.github.io/DependencyCheck/
    # more about the cli commands https://dependency-check.github.io/DependencyCheck/dependency-check-cli/arguments.html
    - >
      sh /usr/dependency-check/bin/dependency-check.sh \
      --project "EnviDat-Frontend" \
      --scan "." \
      --out "reports" \
      --format "HTML" \
      --format "JSON" \
      --failOnCVSS 7 \
      --exclude "**/node_modules/**" \
      --enableExperimental \
      --enableRetired \
      --nvdApiKey "$NVD_API_KEY"
  artifacts:
    paths:
      - reports
  retry:
    max: 2
  volumes:
    - dependency-check-data:/usr/share/dependency-check/data

