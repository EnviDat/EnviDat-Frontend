/* eslint-disable no-unused-vars */

export enum WorkflowMode {
  Create = 'create',
  Edit = 'edit',
}

export enum StepStatus {
  Disabled = 'disabled',
  Active = 'active',
  Completed = 'completed',
  Error = 'error',
}
